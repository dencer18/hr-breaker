services:
  - type: web
    name: hr-breaker
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run src/hr_breaker/main.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"